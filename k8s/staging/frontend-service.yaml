apiVersion: v1
kind: Service
metadata:
  name: frontend-dashboard
  namespace: staging
  labels:
    app: frontend-dashboard
    environment: staging
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: frontend-dashboard
    environment: staging
